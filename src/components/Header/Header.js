import React from "react";
import "./Header.css";

const Header = props => (
  <div className="hero text-center">
            <h2><em>Clicky Game!</em></h2>
            <br></br>
            <h6>click an image to increase your score. If you click the same image twice its GAME OVER</h6>
            
  </div>
);

export default Header;
