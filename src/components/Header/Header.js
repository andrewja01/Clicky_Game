import React from 'react';
import './Header.css';

const Header = props => (
  <div className='hero text-center col-xl row align-items-center justify-content-center'>
    <h4>
      click an image to increase your score. If you click the same image twice
      its GAME OVER
    </h4>
  </div>
);

export default Header;
